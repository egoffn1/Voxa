# --- Server Dependencies (если запускаете сервер локально) ---
flask==3.0.0
flask-cors==4.0.0
mistralai==1.2.6
python-dotenv==1.0.0
gunicorn==21.2.0

# --- Client Dependencies ---

# Распознавание речи
SpeechRecognition>=3.10.0

# Сетевые запросы
requests>=2.31.0

# Аудио-ввод (микрофон)
# Требует системную библиотеку PortAudio:
# Bazzite/Fedora: sudo rpm-ostree install portaudio && systemctl reboot
sounddevice>=0.4.6
numpy>=1.20.0

# --- Нейросетевой синтез речи (Silero TTS) ---
# Требуется для качественного человеческого голоса
torch>=2.0.0
torchaudio>=2.0.0
pyyaml>=6.0

# Воспроизведение аудио (для Silero)
simpleaudio>=1.0.4

# --- Резервный синтез речи (офлайн) ---
# Используется если Silero не загрузился или для экономии ресурсов
pyttsx3>=2.90